export type User = {
    id: number;
    name: string;
    avatar: string;
}

export type TravelPlanTag = {
    id: number;
    name: string;
}

export type TravelPlanPhoto = {
    id: number;
    url: string;
}
